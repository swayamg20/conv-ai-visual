# CLEAN INTERRUPTION FIX
# Apply these changes to main.py

### 1. Remove lines 228-252 (bloated interruption check in transcript handler)
### Replace with: NOTHING - delete this entire block

### 2. Replace lines 254-260 (interrupted transcript check)
### FROM:
if is_final and transcript.strip() and llm_pipeline:
    # Check if pipeline was interrupted - if so, skip this transcript
    if state and state.is_interrupted():
        logger.warning("[%s] ⏭️ Skipping final transcript - pipeline was interrupted", pc_id)
        vad_speech_detected[pc_id] = False
        state.clear_interrupt()  # Clear for next utterance
        return

    if not vad_detected:

### TO:
if is_final and transcript.strip() and llm_pipeline:
    if not vad_detected:

### 3. Replace lines 309-356 (LLM processing with bloat)
### FROM:
# Latency tracking: LLM start
t_llm_start = time.time()
logger.info("[%s] ⏱️ LLM request started", pc_id)

# Mark LLM processing as active so interruptions can cancel it
state = interruption_manager.get_state(pc_id)
if state:
    state.start_llm_processing()

# Stream LLM response with tools support for lower latency
llm_response = ""
first_chunk = True
llm_interrupted = False

try:
    async for chunk in voice_sessions[pc_id].chat_with_tools_stream(...):
        # Check for interruption during LLM streaming
        if state and state.is_interrupted():
            logger.warning("[%s] 🛑 LLM INTERRUPTED - stopping generation", pc_id)
            llm_interrupted = True
            break
        ...
except asyncio.CancelledError:
    logger.warning("[%s] 🛑 LLM task cancelled", pc_id)
    llm_interrupted = True
    raise
finally:
    # Mark LLM processing as complete
    if state:
        state.stop_llm_processing()

# If interrupted during LLM, skip TTS
if llm_interrupted:
    logger.warning("[%s] ⏭️ Skipping TTS - LLM was interrupted", pc_id)
    if state:
        state.clear_interrupt()
    return

### TO:
# Latency tracking: LLM start
t_llm_start = time.time()
logger.info("[%s] ⏱️ LLM request started", pc_id)

# Stream LLM response
llm_response = ""
first_chunk = True
async for chunk in voice_sessions[pc_id].chat_with_tools_stream(
    transcript,
    temperature=config.LLM_TEMPERATURE,
    max_tokens=config.LLM_MAX_TOKENS
):
    if first_chunk:
        t_llm_first_chunk = time.time()
        delta_to_first = (t_llm_first_chunk - t_llm_start) * 1000
        delta_total = (t_llm_first_chunk - t_final_transcript) * 1000
        logger.info("[%s] ⏱️ LLM first chunk: %.0fms (request→chunk), %.0fms (transcript→chunk)", pc_id, delta_to_first, delta_total)
        first_chunk = False
    llm_response += chunk

### 4. Replace lines 474-491 (VAD first frame interrupt check)
### FROM:
# IMMEDIATELY trigger interruption and notify client if pipeline is active (LLM or TTS)
state = interruption_manager.get_state(pc_id)
if state and state.is_pipeline_active() and not state.interrupt_ack_sent:
    logger.warning("[%s] 🎙️ VAD first frame - Speech DURING PIPELINE - INTERRUPTING NOW!", pc_id)
    # signal_interrupt returns False if in grace period
    if state.signal_interrupt():
        # Immediately notify client to stop audio (don't wait for transcript)
        ch = datachannels.get(pc_id)
        if ch and ch.readyState == "open":
            interrupt_ack = json.dumps({
                "type": "interruption_ack",
                "message": "User speaking - stopping response"
            })
            ch.send(interrupt_ack)
            state.interrupt_ack_sent = True
            logger.info("[%s] 📤 Sent immediate interruption_ack to client", pc_id)
elif state:
    logger.debug("[%s] VAD first frame - Speech detected (pipeline_active=%s)", pc_id, state.is_pipeline_active())

### TO:
# Interrupt if TTS is active
state = interruption_manager.get_state(pc_id)
if state and state.tts_active:
    logger.warning("[%s] 🎙️ Speech during TTS - interrupting", pc_id)
    state.signal_interrupt()
    ch = datachannels.get(pc_id)
    if ch and ch.readyState == "open":
        ch.send(json.dumps({"type": "interruption_ack"}))

### 5. Replace lines 511-528 (VAD loop interrupt check)
### Same as #4 - replace bloated check with simple TTS check
