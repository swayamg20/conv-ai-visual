# Experiments

This directory contains research code that is not imported by the Murmur application. Install the optional `experiments` dependency group before running it:

    uv sync --extra experiments

Experiment outputs and model-generated audio belong under the ignored `var/` directory, not in version control.
