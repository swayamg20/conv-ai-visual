# Documentation archive

These files record earlier product ideas, integration experiments, launch assessments, and architecture iterations. They are preserved for context, but they are not maintained against the active codebase.

Expect references to the former `voiceai` name, the deleted `funcs` package, old client pages, obsolete dependencies, and features that may have changed or never shipped.

Use the parent [documentation index](../README.md) for current setup and architecture.
