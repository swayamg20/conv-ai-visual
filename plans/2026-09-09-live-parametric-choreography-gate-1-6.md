# Turn a typed equation into a live verified visual explanation

## Purpose / Big Picture

Gate 1.5 proves that Murmur can play one polished, interruptible completing-square lesson for the fixed equation `x² + 6x = 7`. Gate 1.6, called Live Parametric Choreography, turns that authored example into a real product capability. A signed-in learner opens `/canvas/generate`, types a supported equation such as `x² + 8x = 20`, and sees the correct square, half-coefficient, missing corner, balanced equation, factorization, and roots animate on the board. The learner can stop at a settled checkpoint, ask why the corner is 16, and continue from the exact visible state without restarting or changing the problem.

This gate deliberately combines two speeds. The Visual Reflex is a deterministic, zero-provider path selected explicitly by a V3 request carrying one closed advance-or-clarify route. The Director is a small model-routed path selected explicitly for a less rigid pedagogical request. Both selections are untrusted intent and are validated against the parsed problem and accepted state. Both paths end at the same server-owned route, deterministic compiler, independent verifier, certificate chain, complete-suffix preflight, browser decoder, and checkpoint renderer. Neither the browser nor the model may author coefficients, derived mathematics, SVG coordinates, narration, timing, viewport poses, node identifiers, or integrity evidence.

The supported family is finite and structural rather than a list of showcase equations. The server accepts one monic equation `x² + bx = c`, written with either `x²` or `x^2`, in a dedicated untrusted `problemText` request field when `b` is an even integer from 2 through 16, `c` is a positive integer no greater than 80, and `c + (b/2)²` is a perfect square whose positive root is at most 9. Equivalently, the family is `x² + 2hx = m² - h²` for integers `1 <= h < m <= 9`, which contains 36 equations. These bounds keep every derived quantity and label visually qualifiable while proving that the engine is actually parametric. The compiler and verifier each derive `h`, `h²`, `m²`, `m`, `m-h`, and `-(m+h)` independently.

The visual quality bar rises with the generalization. The main path should settle in 45 to 55 seconds at normal speed rather than Gate 1.5's roughly 64 seconds. Every checkpoint must remain understandable with audio muted: the equation tokens, geometric dimensions, balance operation, factorization, and two roots carry the reasoning directly, while narration reinforces rather than supplies missing logic. Motion should keep the same visual objects through each transformation, use the camera only to focus attention, and preserve the interruption semantics already proven in Gate 1.5.

Gate 1.6 does not attempt arbitrary algebra, arbitrary functions, model-authored animation code, general-purpose Manim compatibility, a new voice pipeline, or cross-session persistence. It establishes one trustworthy parameterized visual grammar and exposes it through the authenticated product surface. Voice remains a parallel track and is not changed in this worktree.

## Progress

- [x] 2026-09-09 05:58 IST: Created clean branch `codex/gate16-live-parametric-choreography` from exact `origin/main` commit `a2604b4`; confirmed the separate main worktree contains voice work and will remain untouched.
- [x] 2026-09-09 06:05 IST: Audited the Gate 1.5 compiler, independent verifier, V2 state and certificate contracts, full-suffix service preflight, authenticated choreography endpoint, strict browser decoder, fixture runner, playback runtime, and current `/canvas/generate` product route.
- [x] 2026-09-09 06:08 IST: Chose an additive V3 protocol, a 36-equation bounded family, and deterministic problem binding before provider resolution; preserved the exact Gate 1.5 V2 fixture, hashes, node IDs, and compiler path as compatibility surfaces.
- [x] 2026-09-09 06:32 IST: Implemented the bounded problem contract and prompt-free parser/binder, stable problem decline categories, V3 routed beat/hash, and problem-bound V3 semantic state. All 36 problems in both accepted notations, every legal state frontier, strict wire rejection, V2 golden hashes, and the unchanged Gate 1.5 fixture pass in 776 focused tests; Ruff, formatting, and diff checks are clean, and the legacy fixture remains `1df951328558d537347625419f9ccf16b454d06f45bd108fc79e86f89ee98887`.
- [x] 2026-09-09 07:27 IST: Added the separate V3 checkpoint receipt and compiler-certificate integrity contracts. Both commitments bind the exact problem hash, component, checkpoint, routed beat, patch, presentation, choreography, state revisions, and prior certificate head; V3-only obligations cover problem identity, caption facts, and authored timing without changing V2. The focused V2/V3 integrity suite passes 97 tests locally, including cross-problem transplant and fixed V2 hash regressions; Ruff, formatting, and diff checks are clean.
- [x] 2026-09-09 07:44 IST: Implemented the deterministic parametric checkpoint-blueprint compiler across all 36 equations, all nine main frontiers, and the atomic corner detour. The board exposes halving, both corner dimensions, `h x h = h^2`, equal balancing, the completed square, explicit `plus or minus`, and both roots in value-neutral nodes over fixed symbolic geometry. Independent review found and the implementation closed two text collisions, a delimiter-bearing component-ID cue bug, and awkward `1x` rendering. The full live-scene suite passes 1,846 tests; Ruff, formatting, and diff checks are clean; the main authored path is exactly 47 seconds; the Gate 1.5 fixture remains byte-identical.
- [x] 2026-09-09 08:14 IST: Added the independently authored parametric verifier over raw `b` and `c`. It closes token and path styling, visible measurements, presentation, geometry order, label containment, collision, caption, timing, base/result viewport, and all mathematical obligations without importing compiler tables or derived problem helpers. Adversarial review exposed two corner-detail labels below the safe viewport; the compiler now raises those labels while preserving the 47-second sequence. All 36 problems, every checkpoint/frontier, clarification, and legacy compiler/verifier contracts pass 1,366 independently rerun tests; Ruff, format, and diff checks are clean.
- [x] 2026-09-09 08:17 IST: Added the atomic outer V3 checkpoint compiler. It materializes each candidate transition, verifies the full accepted frontier and cross-component legibility, independently verifies every checkpoint, binds problem/beat/patch/presentation/choreography/low-level/semantic hashes into the certificate chain, and returns a batch only after the entire requested suffix succeeds. Exhaustive 36-problem by nine-frontier coverage plus clarification, replay, transplant, revision, namespace, collision, budget, and late-corruption tests pass as part of a 1,746-test V2/V3 compiler-verifier regression suite; Ruff, format, and diff checks are clean.
- [x] 2026-09-09 08:20 IST: Implemented the routing kernel for both Gate 1.6 speeds. Visual Reflex synchronously resolves an explicit closed route with no client surface. Director exposes only version, action, optional closed stage, and closed abstention reason; its prompt quotes the learner request and bound state as untrusted data, parses exactly one NDJSON record, waits for provider end-of-stream, allows one sanitized repair, and closes or cancels owned streams. Missing version/action, cross-variant fields, prompt injection, invalid frontiers, hanging streams, errors after an apparently valid record, and repair exhaustion are covered. The routing plus legacy-router suite passes 1,574 tests; Ruff, format, and diff checks are clean.
- [x] 2026-09-09 08:22 IST: Added a distinct additive V3 event and SSE boundary. Checkpoints expose exact problem, beat, accepted component frontier, receipt, presentation, choreography, revision, and certificate-chain joins; problem/semantic declines and provider/integrity failures use closed reason and code vocabularies with contract-bound retryability. V2 checkpoint and terminal discriminators cannot splice into V3, unknown fields and coercions fail, and encoded UTF-8 events are capped at the exact existing 64 KiB browser budget. The independently rerun V2/V3 contract and wire suite passes 139 tests; Ruff, format, and diff checks are clean.
- [x] 2026-09-09 08:37 IST: Mirrored the V3 problem, beat, semantic state, exact Reflex/Director request, receipt, certificate, compiled checkpoint, and stream event contracts at the browser boundary in additive modules. The decoder independently checks the bounded 36-equation family, exact keys and primitive types, problem/frontier/revision/chain joins, closed terminal vocabularies, retryability, and V2/V3 discriminator isolation while treating SHA-256 values as opaque commitments rather than signatures. Independently rerun focused tests pass 103 cases; full frontend lint and type-check pass; the contributor's full frontend run passes 846 tests across 62 files.
- [x] 2026-09-09 08:42 IST: Implemented the focused V3 orchestration service shared by Visual Reflex and Director. Fresh and continuation frontiers, problem binding, resolved route/problem/component identity, capacity, complete deterministic compilation, independent materialization and verification, certificate joins, every SSE record, and the completed terminal are all checked before checkpoint one may escape. Reflex exhausts all 36 supported equations with zero provider resolution; Director starts only after validation, permits one repair, and closes owned clients on every exit. Positive-revision empty state, swapped Director identity, wrong corner premises, corrupt final checkpoints, late wire failure, and cancellation are covered. The focused service suite passes 94 tests and the independently rerun combined V2/V3 service/compiler/verifier/routing/wire suite passes 3,263 tests; Ruff, format, and diff checks are clean. No paid provider call was made.
- [x] 2026-09-09 08:47 IST: Integrated V3 behind the existing authenticated and loopback-guarded choreography endpoints with exact request-union dispatch, protocol-specific SSE encoding, and the existing admission-lease cleanup boundary. Bodies without `protocol` remain V2, unknown protocols fail with 422 before admission, and the sealed V2 service explicitly rejects a V3 semantic component before provider dispatch. `SceneAuthoringService` adds only a thin delegate to the focused V3 collaborator. The independently rerun API, V2 service, and V3 service suites pass 128 tests, and the post-integration full live-scene matrix passes 4,693 tests; Ruff, format, and diff checks are clean.
- [x] 2026-09-09 09:09 IST: Added the V3 browser transport, pure frontier/playback joins, protocol-neutral renderer evidence player, and focused parametric stream runtime. The runtime sends exact Reflex or Director bodies, refreshes authentication headers for every product request, admits only joined problem/semantic/revision/certificate checkpoints, commits only after the post-paint settlement barrier, stops on a two-second fail-closed interruption deadline, rejects post-checkpoint failure terminals, discards stale generations, restores the last accepted board on decode/transport/renderer failure, and preflights the complete retained ledger before zero-network Replay. The shared V2 planner received only a typed V3 adapter. Independently rerun focused tests pass 40 cases and the full frontend suite passes 875 tests across 64 files; full lint and type-check pass.
- [x] Implement the parametric compiler, independently authored verifier, problem-bound V3 receipt and certificate, and all-prefix resume/clarification coverage.
- [x] Implement the zero-provider Visual Reflex and the choreography-only Director, then integrate both with the existing authenticated service while retaining full-suffix atomic preflight.
- [ ] Mirror V3 contracts in the browser, expose editable equation input on `/canvas/generate`, and preserve exact checkpoint interruption, continuation, replay, and error behavior.
- [ ] Shorten and visually qualify the authored pacing, add mute-first evidence, generate independent parametric fixtures, and retain the Gate 1.5 fixture byte-for-byte.
- [ ] Run focused and full backend/frontend checks, provider-free routing evidence, browser evidence, independent review, and CI; after fresh product-owner approval, optionally run the separately capped Azure corpus and record exact evidence before requesting merge.

## Surprises & Discoveries

- The authenticated choreography HTTP route already exists at `POST /api/live-scenes/choreography/stream`, and the browser transport already supports it. The product route still mounts the older Pythagorean `LiveModelScene`, while the Gate 1.5 completing-square UI defaults to a provider-free fixture. Gate 1.6 therefore needs product wiring and V3 decoding, not a new transport stack.
- The current shared visual-router prompt advertises only Pythagorean decisions even though its parser union contains choreography decisions. Sending the Gate 1.5 choreography service to a real provider is therefore likely to produce a valid Pythagorean decision that the service safely declines. Gate 1.6 needs a choreography-only model boundary rather than another prompt branch in the shared system prompt.
- Gate 1.5's fixed values appear independently throughout both `completing_square_compiler.py` and `completing_square_verifier.py`, and several node IDs embed those values. Reinterpreting the V2 types would silently invalidate their semantic meaning and hashes. The parametric path must use value-neutral IDs and new V3 integrity envelopes.
- The existing service preflights the entire requested checkpoint suffix before emitting checkpoint one. That stronger atomicity is more valuable than speculative first paint: a later corrupt checkpoint cannot leave a learner with a certified but incomplete misleading prefix. Visual Reflex will reduce routing latency without weakening that guarantee.
- The existing choreography endpoint accepts the unversioned `SemanticLiveSceneRequest`, and several sealed V2 tests intentionally omit an equation. Treating all such requests as parametric would make empty-state dispatch ambiguous and break compatibility. Gate 1.6 therefore needs an explicit request discriminator and a separate service method even if both protocols share one authenticated URL.
- Gate 1.5 has no audio track; its server narration appears as an on-screen caption. A meaningful visual-comprehension test must hide the caption and inspect the board alone rather than merely muting media.
- The product needs an editable equation control and a separate pedagogical prompt. Hiding the equation inside `prompt` would make quoted equations, continuation reuse, and conflict detection ambiguous. V3 therefore parses only `problemText`; Director interprets only `prompt`.
- The area model is symbolic algebra, not a metric drawing of an unknown signed value. Scaling the drawn `x` side from a solved positive root would be circular and would visually erase the negative solution. V3 uses clearly labeled not-to-scale tiles and reveals both roots only after the explicit `plus or minus` algebra step.
- The existing checkpoint certificates are domain-separated, unkeyed SHA-256 consistency commitments, not cryptographic signatures. Server-side independent realization checks make a supplied frontier safe to continue, but the hash chain alone does not authenticate who produced it or prevent a client from submitting another valid frontier. Gate 1.6 does not claim stronger provenance; session-bound HMAC or persisted ownership can be a later gate if stage-skipping becomes consequential.
- Independent collision review caught overlaps that viewport-containment tests alone could not see: the scale note intersected the corner calculation and the final root row touched a dimension label. Gate 1.6 therefore exhausts pairwise visible-text collision checks across every supported problem and checkpoint instead of treating in-bounds geometry as readable geometry.
- Independent verifier hardening showed that checking only camera focus and emphasis targets is insufficient: ordinary mathematical facts can be clipped while the highlighted object remains visible. Gate 1.6 therefore verifies an independently authored full fact set against both base and result viewports in cinematic and compact layouts. This immediately found the corner dimension and calculation labels below the safe bottom edge, which were moved upward before certification.
- Compiling a valid component in isolation is not sufficient when another accepted component already occupies the board. The outer V3 compiler therefore checks the completed candidate scene for positive-area collisions between the target namespace and every foreign node, while allowing deliberate edge contact; any such conflict aborts the complete suffix before an event can be emitted.
- Receiving one syntactically valid Director record is not enough to prove that the provider obeyed the exact-one-record contract: a later record or error may still arrive. The Director keeps acceptance provisional until provider end-of-stream, so a valid record followed by a hang becomes a timeout and a valid record followed by an upstream exception becomes a sanitized provider error.

## Decision Log

- Decision: implement Gate 1.6 in the isolated worktree `/Users/swayam.gupta/Documents/GitHub/conv-ai-visual-scene-product` and leave `/Users/swayam.gupta/Documents/GitHub/conv-ai-visual` untouched. Rationale: the latter contains concurrent voice work, and the user explicitly separated the visual and voice tracks. Date/Author: 2026-09-09 / Codex.
- Decision: retain all V2 contracts and add V3 siblings instead of adding optional problem fields to V2. Rationale: V2 routed-beat hashes, certificates, semantic state, strict browser decoders, and the checked-in Gate 1.5 fixture are already compatibility surfaces. Date/Author: 2026-09-09 / Codex.
- Decision: support the complete bounded family `1 <= h < m <= 9`, expressed as `x² + 2hx = m² - h²`, rather than only three catalog entries. Rationale: a structural finite domain proves real parameterization while keeping the layout, label sizes, mathematical outputs, and exhaustive tests bounded. Date/Author: 2026-09-09 / Codex.
- Decision: parse and bind the equation deterministically before resolving a provider. On an empty frontier exactly one supported equation is required; on a V3 continuation no equation reuses the committed spec, the same equation is allowed, and a different or multiple equation declines without provider dispatch. Rationale: the model may choose pedagogical direction but must never choose the mathematics. Date/Author: 2026-09-09 / Codex.
- Decision: make Visual Reflex a deterministic route selected by an explicit closed request field, not inferred from prompt phrasing and not emitted as a speculative checkpoint. All output still waits for complete compile, independent verification, certificate construction, and wire preflight. Rationale: this removes avoidable provider latency while preserving all-or-nothing correctness. Date/Author: 2026-09-09 / Codex.
- Decision: use a choreography-only decision schema and prompt for Director requests. The only model choices are start, continue, clarify-corner, target stage, or abstain. Rationale: domain separation prevents Pythagorean decisions and excludes equations, narration, geometry, timing, and integrity data from the model boundary. Date/Author: 2026-09-09 / Codex.
- Decision: preserve the eight main checkpoint identities and single `corner_detail` detour while shortening their authored holds to a 45-to-55-second main path. Rationale: these are useful semantic boundaries and replay units; the user feedback identified pacing, not checkpoint meaning, as the weakness. Date/Author: 2026-09-09 / Codex.
- Decision: add exact-wire `ParametricChoreographyRequestV3` with an explicit `protocol` discriminator and a separate parametric service method. Requests without that discriminator continue through the sealed V2 method; V3 product requests use the new path. Rationale: an empty semantic frontier cannot otherwise distinguish legacy fixed choreography from a parametric start, and compatibility tests must not be reinterpreted. Date/Author: 2026-09-09 / Codex.
- Decision: split the V3 request into exact `routingMode: "reflex"` and `routingMode: "director"` variants. Reflex requires a closed requested route; Director forbids it. Rationale: explicit selection is testable, avoids magical intent regexes and prompt-injection ambiguity, and lets the paid corpus prove every case actually dispatched to the model. Date/Author: 2026-09-09 / Codex.
- Decision: add V3-specific stable decline codes `problem_required`, `problem_ambiguous`, `problem_unsupported`, and `problem_conflict` alongside semantic `unsupported_intent` and `no_forward_progress`. A corner question that asserts the wrong numeric value is `problem_conflict` and must never silently animate the asserted value. Rationale: the authenticated UI can present precise server-owned feedback without reimplementing the parser. Date/Author: 2026-09-09 / Codex.
- Decision: carry the equation in `problemText`, separate from Director-only `prompt`. A Reflex request needs no pedagogical prompt because its closed route is complete; a Director request requires one. Rationale: mathematical identity and pedagogical intent have different trust and lifecycle rules. Date/Author: 2026-09-09 / Codex.
- Decision: keep the area tiles symbolically proportioned and explicitly not to scale; vary their labels and equations, not their geometry according to a solved root. Rationale: `x` is the unknown, may be negative algebraically, and must not be derived visually from the answer. Date/Author: 2026-09-09 / Codex.
- Decision: describe V3 hashes as integrity and consistency commitments, never as server-authenticated signatures. Rationale: canonical SHA-256 plus independent verification proves exact joins and realization, not provenance. Date/Author: 2026-09-09 / Codex.
- Decision: put V3 problem, compiler, verifier, checkpoint envelope, router, and service-preflight behavior in focused new modules. Existing `service.py` and the browser runtime receive only narrow delegation/version-dispatch changes. Rationale: those files are already large, and Gate 1.6 should add capability without creating new god modules. Date/Author: 2026-09-09 / Codex.
- Decision: make the V3 verifier own a closed visible-token and visible-path vocabulary, plus independently authored viewport subject sets for every checkpoint. Rationale: mathematical equality is not enough if a valid-looking patch can hide, shrink, clip, or self-intersect the facts a learner must see. Date/Author: 2026-09-09 / Codex.
- Decision: do not run a paid model corpus until provider-free correctness, a clean pushed commit, a dry-run cost reservation, an exact prompt snapshot, and fresh explicit Gate 1.6 spend approval all exist. The proposed campaign cap is USD 0.07; prior-gate approval is not reused. Date/Author: 2026-09-09 / Codex.

## Outcomes & Retrospective

Implementation is in progress. This section will record the shipped branch and merge commits, exact test counts, V2 compatibility digests, V3 fixture digests, live-provider spend and latency, browser evidence, product-owner findings, and remaining limitations. Gate 1.6 is not complete merely because the parametric compiler works; it is complete only when a signed-in user can edit the equation on `/canvas/generate`, receive the correct live lesson, interrupt and clarify the matching corner, continue and replay, and the full proof below passes.

## Context and Orientation

The low-level scene protocol lives in `backend/murmur/live_scene/contracts.py`. It defines immutable SVG-oriented nodes, scene patches, scene revisions, and lifecycle events. `backend/murmur/live_scene/semantic_contracts.py` adds server-owned semantic component state. Gate 1.5's fixed completing-square frontier is `CompletingSquareState` in `backend/murmur/live_scene/completing_square_contracts.py`; it stores only the component identity, last main checkpoint, and whether the corner clarification has happened.

`backend/murmur/live_scene/choreography_contracts.py` defines the V2 routed beat and the reusable V1 presentation vocabulary: stages, checkpoint captions, exact cinematic and compact viewport poses, and one bounded parallel cue phase. `backend/murmur/live_scene/completing_square_compiler.py` turns a routed V2 beat into a deterministic sequence of checkpoint patches. `backend/murmur/live_scene/completing_square_verifier.py` independently reconstructs the expected mathematics and visual obligations without importing compiler snapshots. `backend/murmur/live_scene/checkpoint_contracts.py` binds the routed beat, patch, independent receipt, presentation, choreography, low-level and semantic state hashes, and previous certificate head into a V2 checkpoint certificate.

`backend/murmur/live_scene/service.py` currently routes a choreography request through `VisualActRoutingEngine`, lowers the model decision to a V2 beat, compiles it, and preflights the complete suffix through `_prepare_choreography_batch` before emitting a checkpoint. The service must retain that ordering. `backend/murmur/api/routers/live_scenes.py` exposes the authenticated product endpoint and a separately guarded loopback lab endpoint, both using `SemanticLiveSceneRequest` and the 64 KiB choreography wire encoder.

In the browser, `web/src/lib/live-scene/choreography.ts` and `web/src/lib/live-scene/checkpoint.ts` strictly decode V2 compiler claims. `web/src/features/live-scene/choreography-model-stream.ts` decodes the complete stream envelope and calls the product endpoint. `choreography-playback.ts`, `choreography-stream-runtime.ts`, `choreography-executor.ts`, and `svg-node-reconciler.ts` validate, queue, animate, settle, interrupt, and replay checkpoint transactions. `live-choreography-demo.tsx` is the Gate 1.5 fixture-oriented UI. The user-facing page `web/src/app/(app)/canvas/generate/page.tsx` currently renders `LiveModelScene`, which calls the older semantic Pythagorean endpoint.

A presentation checkpoint is a complete settled teaching unit: its patch changes the board, its choreography describes permitted motion over that patch, its caption explains that step, and its certificate binds the transition. A frontier is the last accepted low-level and semantic state plus its certificate-chain head. Full-suffix preflight means the server validates every checkpoint requested by one action before it emits any of them. These definitions remain unchanged in V3.

## Plan of Work

First, add a dependency-leaf problem module rather than creating a state/beat import cycle. `backend/murmur/live_scene/completing_square_problem_contracts.py` will define exact-wire `CompletingSquareProblemSpecV1` with only `v`, `linearCoefficient`, and `rightHandSide`, validate the bounded family, expose compiler-facing derived values, and hash the spec under `murmur:completing-square-problem:v1`. `completing_square_problem_parser.py` will parse exactly one canonical symbolic equation from `problemText` and distinguish absent, multiple, malformed, conflicting, and unsupported cases without retaining arbitrary text. Its exhaustive test iterates the full 36-case family and every neighboring invalid boundary. The verifier will read only primitive `linearCoefficient` and `rightHandSide` and redo the arithmetic itself; it will not consume the compiler-facing derived helper.

Second, add V3 state and routing contracts without changing V2. `ParametricCompletingSquareStateV1` uses kind `completing_square_parametric` and stores the exact problem spec with the existing checkpoint frontier and clarification bit. `RoutedChoreographyBeatV3` uses hash domain `murmur:routed-choreography-beat:v3`, includes the problem spec, and reuses the existing closed advance/clarify route vocabulary. `SemanticSceneState` accepts both V2 and V3 component kinds. Continuation can never auto-upgrade or splice a V2 frontier into V3.

Third, implement a parametric compiler in a focused new module using value-neutral node IDs such as `eq_linear`, `eq_half_a`, `eq_half_b`, `eq_corner_value`, `eq_completed_rhs`, `root_positive`, and `root_negative`. Extract a shared helper from the V2 compiler only if byte-identical V2 fixture tests prove the refactor; otherwise keep the V3 implementation independent. Leave every V2 constant, ID, hash, and serialized output unchanged. Derive display strings and bounded token widths from the spec. Keep the square, equal strips, and missing corner in one certified symbolic not-to-scale geometry for all problems; parameterize the dimension and area labels rather than pretending `x` has a known metric length. Keep both cinematic and compact focus poses unclipped for all 36 problems. Replace narration-only reasoning with visible dimension labels, an explicit `h x + h x = 2hx` split, `h x h = h²`, `c + h² = m²`, `(x+h)² = m²`, then `x+h = plus or minus m`, and only then the two exact roots.

Fourth, add an independently authored V3 verifier and integrity envelope in new modules. `CheckpointVerificationReceiptV3`, `CheckpointCompilerCertificateBodyV3`, `CheckpointCompilerCertificateV3`, and `CompiledCheckpointV3` use V3-specific hash domains and compiler version `murmur.completing_square_choreography.v2`. Both receipt and certificate body contain `problemSpecSha256`; validators prove that beat spec, accepted component spec, result component spec, receipt digest, and certificate digest all agree. The verifier independently derives the allowed equations, geometry, operation targets, camera containment, factorization, and roots and must retain an AST test forbidding compiler imports or a shared expected-checkpoint table. Tests transplant receipts and certificates across problems and corrupt later checkpoints to prove zero events escape preflight.

Fifth, separate routing into the Visual Reflex and Director. A small `parametric_choreography_routing` module accepts an explicit closed route only from a `routingMode: "reflex"` request and resolves it against the server-bound problem and semantic frontier with zero provider calls. A `routingMode: "director"` request forbids a client route and goes through a choreography-only prompt, strict single-record parser, and one-repair engine. The Director sees untrusted prompt text and the semantic frontier but can emit only start, continue, clarify, stage, or abstain; the parsed problem stays outside its output. Unsupported, missing, multiple, or conflicting equations produce distinct stable decline codes before the client factory and provider admission. A clarification prompt containing a numeric corner claim must either match the derived corner or decline as `problem_conflict`. Request admission still happens first so malformed use remains rate bounded. Route-specific capacity errors become non-retryable for an unchanged request.

Sixth, add a discriminated V3 request union sharing `protocol: "parametric_choreography_v3"`, optional `problemText`, `generation`, `baseScene`, and `baseSemanticScene`. Its Reflex variant has `routingMode: "reflex"` plus required closed `requestedRoute` and forbids `prompt`; its Director variant has `routingMode: "director"`, requires `prompt`, and forbids a client route. A fresh start requires `problemText`; a continuation may omit it to reuse the committed spec, may repeat the same problem, and rejects a different problem. The authenticated choreography endpoint dispatches these requests to a new focused parametric service collaborator; bodies without `protocol` retain the existing V2 request and service behavior. The V3 service order is authentication and request admission, started event, paired-base/capacity validation, deterministic problem binding, reflex-or-provider routing, V3 lowering, compile, independent verify, certificate binding, and complete wire preflight before checkpoint one. `service.py` delegates through a narrow method instead of absorbing the implementation. Cancellation and disconnect close exactly the owned provider stream and request lease. Service tests assert zero client-factory and provider calls on pre-provider declines and mismatches.

Seventh, mirror V3 at the browser boundary in new problem, choreography-V3, and checkpoint-V3 modules. Add exact-key problem, beat, component, receipt, certificate, compiled-checkpoint, and stream decoders selected by `beat.v`; do not loosen the V2 decoder. Generalize the existing runtime only at a narrow version-neutral checkpoint adapter where both versions have the same safe presentation behavior; keep V3 joins and state application outside the already-large runtime module. The browser validates all cleartext joins and chain heads, treats server hashes as opaque commitments, rejects a V2/V3 splice in one ledger, and retains the last accepted frontier on any V3 decode or playback failure.

Eighth, add a focused authenticated product wrapper instead of turning `live-choreography-demo.tsx` into a god component. The wrapper owns editable equation text, examples, auth headers, product transport, reset-on-problem-change behavior, and user-facing parser/decline messages. The existing stage/runtime remains responsible for playback. `web/src/app/(app)/canvas/generate/page.tsx` renders this wrapper. A problem may be edited before starting or after an explicit reset, never while a certified frontier exists. Buttons issue the exact reflex grammar; free-form pedagogical text exercises Director. The corner button displays the derived value, and captions/progress update from V3 state rather than fixed constants.

Ninth, qualify speed and comprehension. Reduce the main authored timing to 45 through 55 seconds without changing checkpoint settlement semantics. Generate separate deterministic fixtures and a separate V3 capture route for `x² + 2x = 80`, `x² + 8x = 20`, and `x² + 16x = 17`; together they stress the smallest corner with the largest right side, the primary product example, and the largest coefficient/corner/negative root. Leave the Gate 1.5 route, artifact validator, and `completing-the-square.v1.json` byte-identical at SHA-256 `1df951328558d537347625419f9ccf16b454d06f45bd108fc79e86f89ee98887`. The V3 Playwright suite covers equation editing, all eight checkpoints, corner clarification for 16, mid-motion interruption, exact continuation, zero-request replay, compact layout, reduced motion, unsupported input, and reset removal of every value-neutral node. A caption-hidden board-only capture is reviewed against six visible facts: halve `b`, form the `h²` corner, add that amount to both sides, obtain `m²`, pass through `plus or minus m`, and derive both roots. Shape, labels, and motion must carry meaning without relying on color alone.

Finally, qualify the model boundary and release. Provider-free tests must pass on a clean pushed commit before any paid request. After fresh explicit Gate 1.6 spend approval, the optional live Azure corpus will contain explicit `routingMode: "director"` starts, resumes, clarification, no-progress, and unsupported-intent decisions whose pedagogical prompts differ from the closed Reflex button labels. It allows at most 15 cases, 30 dispatches including repair, 2,048 output tokens per dispatch, SDK retries disabled, at least 6.1 seconds between dispatches, and a proposed conservative USD 0.07 pre-dispatch reservation cap. No secrets or raw prompts enter committed artifacts. After the full local matrix and CI pass, inspect the real-speed video, caption-hidden board-only capture, and settled screenshots, record findings in this plan, and present the branch for product-owner testing before merge.

## Concrete Steps

All commands run from `/Users/swayam.gupta/Documents/GitHub/conv-ai-visual-scene-product` unless they begin with `cd web`.

Before and after every milestone, prove the branch and worktree:

    git status --short --branch
    git rev-parse HEAD
    git rev-parse @{upstream}

After the bounded problem and V3 state/beat contract milestone:

    uv run pytest -q tests/test_live_scene_completing_square_problem.py tests/test_live_scene_completing_square_contracts.py tests/test_live_scene_choreography_contracts.py tests/test_live_scene_semantic_contracts.py tests/test_live_scene_semantic_integrity.py
    uv run ruff check backend/murmur/live_scene tests/test_live_scene_completing_square_problem.py tests/test_live_scene_completing_square_contracts.py tests/test_live_scene_choreography_contracts.py
    uv run ruff format --check backend/murmur/live_scene tests

After the V3 compiler, verifier, receipt, and certificate milestone:

    uv run pytest -q tests/test_live_scene_completing_square_compiler.py tests/test_live_scene_completing_square_verifier.py tests/test_live_scene_checkpoint_contracts.py tests/test_live_scene_choreography_contracts.py tests/test_live_scene_choreography_fixture.py
    uv run ruff check backend/murmur/live_scene tests/test_live_scene_completing_square_compiler.py tests/test_live_scene_completing_square_verifier.py tests/test_live_scene_checkpoint_contracts.py
    uv run ruff format --check backend/murmur/live_scene tests

After routing, service, API, and zero-provider reflex integration:

    uv run pytest -q tests/test_live_scene_choreography_router.py tests/test_live_scene_choreography_router_prompt.py tests/test_live_scene_visual_act_engine.py tests/test_live_scene_visual_act_lowering.py tests/test_live_scene_routed_choreography_service.py tests/test_live_scene_choreography_service_contracts.py tests/test_live_scene_choreography_wire.py tests/test_live_scene_choreography_api.py tests/test_live_scene_admission.py
    uv run pytest -q tests/test_live_scene_visual_act_router_prompt.py tests/test_live_scene_routed_semantic_service.py tests/test_live_scene_semantic_api.py
    uv run ruff check backend/murmur/live_scene backend/murmur/api/routers/live_scenes.py tests
    uv run ruff format --check backend/murmur/live_scene backend/murmur/api/routers/live_scenes.py tests

Generate V3 fixtures only through a deterministic repository script. Generate twice to separate temporary paths and compare SHA-256 values before replacing any checked-in V3 fixture. Never pass the Gate 1.5 V1 fixture path as an output target.

After browser contracts, runtime, and product UI:

    cd web && npm test -- src/lib/live-scene/choreography.test.ts src/lib/live-scene/checkpoint.test.ts src/lib/live-scene/choreography-planner.test.ts src/features/live-scene/choreography-model-stream.test.ts src/features/live-scene/choreography-playback.test.ts src/features/live-scene/choreography-stream-runtime.test.ts src/features/live-scene/live-parametric-choreography.test.tsx
    cd web && npm run lint
    cd web && npm run typecheck

Run provider-free browser evidence and the normal-speed capture:

    cd web && npm run e2e:choreography
    cd web && npm run capture:choreography
    cd web && npm run validate:choreography-artifacts

Before any paid routing corpus, obtain fresh explicit Gate 1.6 spend approval. Immediately recalculate a conservative reservation from the deployed Azure model's current input, output, and reasoning-token price rather than assuming 30 maximum-size dispatches fit within USD 0.07. Then run dry-run mode and verify that the resolved git commit is clean, the exact prompt snapshot is hashed, the output ceiling is the smallest practical size for one short decision, all reservations fit below the newly approved cap, SDK retries are zero, every dispatch including repair is paced, and no Director case accidentally takes Reflex. Run the paid mode once only if all guards pass, then validate its private sanitized report.

Before each coherent milestone commit:

    git diff --check
    git status --short

Push each milestone immediately to the feature branch:

    git push -u origin codex/gate16-live-parametric-choreography
    git rev-parse HEAD
    git rev-parse @{upstream}

Before requesting merge, run the complete local gate:

    uv run pytest -q
    uv run ruff check .
    uv run ruff format --check .
    cd web && npm test
    cd web && npm run lint
    cd web && npm run typecheck
    cd web && npm run build
    cd web && npm run e2e:scene
    cd web && npm run e2e:choreography
    cd web && npm run capture:choreography
    cd web && npm run validate:choreography-artifacts
    git diff --check

Record exact counts, durations, digests, live cost, timing percentiles, artifact paths, and any nonblocking caveats in `Progress` and `Outcomes & Retrospective`. Verify the pushed SHA and GitHub Actions run rather than treating a local commit as delivery.

## Validation and Acceptance

The problem boundary passes when every one of the 36 supported equations parses from both `x²` and `x^2` notation and derives the expected half-coefficient, corner, completed right side, square-root magnitude, and two roots. Empty-state prompts with no equation, multiple equations, malformed syntax, a non-monic square, another variable, an odd or out-of-range linear coefficient, a nonpositive or out-of-range right side, a non-square completion, or a conflicting continuation all decline with the exact V3 problem code and without client-factory or provider dispatch. A numeric corner assertion must match the derived corner; a wrong premise receives `problem_conflict` and no mutation.

V3 compatibility passes when the exact V2 routed-beat golden hash still matches, the Gate 1.5 fixture remains byte-identical at its recorded SHA-256, every V2 test remains unchanged and green, and no V2 semantic frontier can be continued as V3 or vice versa. V3 serialization must reject unknown fields, non-strict integers, invalid problem digests, changed problem identity, and cross-problem receipt or certificate replay. These checks prove integrity and consistency of the accepted state; Gate 1.6 does not claim that an unkeyed digest authenticates server provenance.

The compiler and verifier pass when every one of the 36 supported equations compiles deterministically from empty state through all eight main checkpoints, resumes from each of its nine possible main frontiers, and accepts the corner clarification exactly once at its unclarified `missing_corner` frontier. Every displayed value and symbolic dimension must derive from the problem. The independent verifier consumes only primitive `b` and `c`, recomputes all derived values, and rejects a wrong or missing visible caption fact, wrong split, wrong corner area, unequal balance, wrong completed square, wrong factor, either wrong root, clipped focus, collision, illegal morph, foreign node, changed problem, or later-checkpoint corruption. Any failure during complete-suffix preflight emits zero checkpoint events.

Routing passes when an explicit Reflex request with a valid closed route makes exactly zero client-factory and provider calls, every parser/problem decline also makes zero, and an explicit Director request makes exactly one normal dispatch with at most one repair through the choreography-only schema. A Reflex request without a route, a Director request with a route, and any unknown field fail request validation. Absent `protocol` preserves the exact legacy V2 body; an unknown protocol fails validation rather than falling back. The model can choose only action and stage. One malformed or state-invalid decision triggers one sanitized repair; a second fails with `invalid_visual_act`. Provider timeout, error, and admission retain their retryable codes; semantic, capacity, compiler, verifier, certificate, problem, and wire failures are non-retryable where repeating the same request cannot help. Tests reject V2-to-V3 and V3-to-V2 frontier attempts, mixed-version checkpoint ledgers, dirty initial boards, and a corrupt final checkpoint while proving zero checkpoint events escaped.

The browser and product pass when an authenticated learner can edit the initial equation on `/canvas/generate`, start `x² + 8x = 20`, watch the correct eight-checkpoint lesson, stop at the missing 4-by-4 corner, ask why it is 16, continue to `(x+4)² = 36` and roots 2 and -10, and replay without a network request. Editing is locked from request start through playback and available again only after reset; reset aborts in-flight work and removes every V3 node. A valid Firebase bearer is acquired for every product request, an unauthenticated request reaches neither service nor provider, and the loopback lab bypass cannot authorize the product route. Decode, renderer, or stream failure retains the last accepted board, while late chunks from an aborted generation are discarded. Compact layout, reduced motion, cancellation, and stale-generation handling remain exact.

The visual gate passes when the normal-speed main path is between 45 and 55 seconds; first visible deterministic feedback is measured separately from authored holds; a caption-hidden board-only capture makes the half-coefficient, corner square, balanced addition, completed square, and roots legible without explanatory copy; the corner zoom remains purposeful; text stays within both certified viewports; object identity and cue order remain stable; interruption settles within the existing latency budget; and replay reproduces the same captions, node order, viewport sequence, and certificate chain with zero live-scene requests.

When separately approved, the live-provider gate passes when all 15 cases terminate safely, all supported route expectations and four resume identities are exact, unsupported/no-progress cases do not mutate, at least 14 of 15 cases succeed without repair, every mutation carries a valid problem-bound certificate chain, warm route median is at most 1.5 seconds and p95 at most 3 seconds, accepted route to first preflighted checkpoint is at most 100 ms p95, dispatch pacing and reservation counts match the report, and total conservative reservation does not exceed the approved cap. Until that approval exists, paid qualification remains explicitly pending rather than silently inherited from another gate.

Gate 1.6 is ready for merge only after the full backend and frontend matrices, production build, Gate 1.4 and Gate 1.5 regression browsers, Gate 1.6 product browser proof, artifact validation, exact pushed-head CI, and product-owner visual test all pass. Merge remains a separate explicit action after that evidence is presented.
